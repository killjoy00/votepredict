name: Revisor source-passage snapshot
on:
  push:
    branches: [model/authoritative-source-passage-labels]
    paths:
      - .github/workflows/revisor-source-passage-snapshot.yml
      - scripts/export-revisor-source-passage-snapshot.ts
      - scripts/audit-revisor-source-passage-search.ts
      - scripts/audit-revisor-passage-discrepancies.ts
      - scripts/audit-revisor-alternate-passage-actions.ts
      - src/sources/minnesota/revisor-action-search.ts
      - src/sources/minnesota/revisor-actions.ts
      - tests/revisor-action-search.test.ts
  workflow_dispatch:
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-fund --no-audit
      - name: Audit all six authoritative source-passage sets
        run: node --import tsx scripts/audit-revisor-source-passage-search.ts
      - name: Audit alternate Senate passage categories
        run: node --import tsx scripts/audit-revisor-alternate-passage-actions.ts
      - name: Audit disputed Senate passage labels
        run: node --import tsx scripts/audit-revisor-passage-discrepancies.ts
      - name: Export audited Revisor source-passage set
        run: node --import tsx scripts/export-revisor-source-passage-snapshot.ts
      - name: Upload machine-readable snapshot
        uses: actions/upload-artifact@v4
        with:
          name: revisor-source-passage-snapshot
          path: artifacts/revisor-source-passage-snapshot.json
          if-no-files-found: error
          retention-days: 7
