# Vercel fallback secret scope

This repository is owned by the personal GitHub account `killjoy00`, not by a GitHub organization.

The fallback workflow expects a repository Actions secret named `VERCEL_TOKEN`. An organization-level secret stored under a different GitHub organization is not inherited by this personal repository.

Never store the Vercel token in a repository variable, source file, issue, pull request, commit message, or chat.
